<%= form_for(@transactio) do |f| %>
  <% if @transactio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transactio.errors.count, "error") %> prohibited this transactio from being saved:</h2>

      <ul>
      <% @transactio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
